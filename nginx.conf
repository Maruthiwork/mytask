server {
    listen 80;
    server_name your_domain_or_ip; 

    location / {
        root /usr/share/nginx/html; 
        index index.html; 
    }

    # Add more locations and configurations as needed
}
